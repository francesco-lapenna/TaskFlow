# Toggle the active datasource. "memory" works without MySQL.
DB_CONNECTOR=memory

# Used when DB_CONNECTOR=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USER=taskflow
DB_PASSWORD=taskflow
DB_DATABASE=taskflow

# REST listener
HOST=0.0.0.0
PORT=3000
