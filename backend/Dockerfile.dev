FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

# source is mounted at runtime via docker-compose
COPY . .

EXPOSE 3000

# build once on container start, then watch + reload
CMD ["npm", "run", "dev"]
